Imports SkyEditorBase

Public Class Commands
    Public Shared Sub MyCommand(Manager As PluginManager, Argument As String)
        'In the console, the usage will be <commandName> <Argument>
        '<commandName> is whatever you define in the plugin definition.
        '<Argument> is passed into the Argument parameter above.
    End Sub
End Class
