<skyWPF:DataBoundObjectControl x:Class="StringParameterEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ROMEditor.UI.WPF"
             xmlns:skyWPF="clr-namespace:SkyEditor.UI.WPF;assembly=SkyEditor.UI.WPF"
             xmlns:script="clr-namespace:SkyEditor.ROMEditor.Windows.FileFormats.Explorers.Script;assembly=SkyEditor.ROMEditor.Windows"
             xmlns:res="clr-namespace:ROMEditor.UI.WPF.My.Resources"
             mc:Ignorable="d" 
             TargetType="{x:Type script:StringCommandParameter}"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0" Text="{x:Static res:Language.Text}" />
        <TextBox Grid.Column="1" AcceptsReturn="True" Text="{Binding English}" />
    </Grid>
</skyWPF:DataBoundObjectControl>
